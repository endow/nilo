# v0.7.6

## リリースノート（日本語）

release recipe が、対応する作業受付タスクを安全に採用して完了まで追跡できるようになりました。

### 変更点

- release work task の採用処理を修正し、リリース実行時に対応する受付タスクを正しく引き継ぐようにしました。

### 検証

- changed check: `PYTHONPATH=src python tests/run_shards.py --changed --jobs auto`（成功）
- 変更範囲: `v0.7.5..HEAD`; 対象 commit: f2b57b0 Fix release work task adoption
- 公開は人間承認後に tag / push / GitHub release を実行する。

## Release Notes (English)

The release recipe can now safely adopt the matching work-intake task and track it through release completion.

### Changes

- Fixed release work-task adoption so a release run correctly carries forward its matching intake task.

### Verification

- Changed check: `PYTHONPATH=src python tests/run_shards.py --changed --jobs auto` (passed)
- Change range: `v0.7.5..HEAD`; commits: f2b57b0 Fix release work task adoption
- Publishing remains gated by explicit human approval for tag, push, and GitHub release creation.
