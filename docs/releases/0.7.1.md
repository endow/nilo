# v0.7.1

## リリースノート（日本語）

Nilo 0.7.1 は、release publish 前の full check gate 復旧を目的にした patch release です。

### 変更点

- release publish の full check gate が、直前の release prepare / resume 状態から確実に復旧できるようにしました。
- release workflow context の復旧処理を強化し、release gate 判定を再開時にも維持するテストを追加しました。

### 検証

- changed check: `PYTHONPATH=src python tests/run_shards.py --changed --jobs auto` -> passed (`verification_eb44e119080b`)
- full check: `PYTHONPATH=src python tests/run_shards.py --all --jobs auto` -> passed (`.nilo/test-runs/20260707T112841276289Z/summary.json`)
- 変更範囲: `v0.7.0..HEAD`; 対象 commit: c574456 Fix release full-check gate recovery
- 公開は人間承認後に tag / push / GitHub release を実行する。

## Release Notes (English)

Nilo 0.7.1 is a patch release for release publish full-check gate recovery.

### Changes

- Restored reliable full-check gate recovery before `release publish` from the preceding release prepare / resume state.
- Strengthened release workflow context recovery and added tests that keep release gate decisions intact after resuming.

### Verification

- changed check: `PYTHONPATH=src python tests/run_shards.py --changed --jobs auto` -> passed (`verification_eb44e119080b`)
- full check: `PYTHONPATH=src python tests/run_shards.py --all --jobs auto` -> passed (`.nilo/test-runs/20260707T112841276289Z/summary.json`)
- Change range: `v0.7.0..HEAD`; commits: c574456 Fix release full-check gate recovery
- Publishing remains gated by explicit human approval for tag, push, and GitHub release creation.
