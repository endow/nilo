# v0.7.8

## リリースノート（日本語）

v0.7.8 では、AI 向け status 表示、レビュー証跡、人間承認メッセージ、失敗ログのフィードバック基盤を改善しました。

### 変更点

- `nilo status --ai` に active roadmap の状態と次アクションを分かりやすく表示するよう改善。
- 人間承認が必要なメッセージを project の `primary_language` に合わせてローカライズ。
- Claude ReviewResult の取り込みと、未実行レビュー・未解決 finding の証跡表示を堅牢化。
- failure fingerprint と shadow report を含む、軽量な失敗ログフィードバック Phase 1 を追加。
- release metadata の検証完了判定と CLI test shard の網羅性を修正。

### 検証

- changed check: `PYTHONPATH=src python tests/run_shards.py --changed --jobs auto` — 成功。
- full check: `PYTHONPATH=src python tests/run_shards.py --all --jobs auto` — 成功（`verification_5298d550d511`）。
- 変更範囲: `v0.7.7..HEAD`; 対象 commit: e8fc80e Improve active roadmap status display; 854fcfb Localize human approval messages; 1375ea7 Harden Claude ReviewResult import; 949b20b Clarify review evidence status; 93a7659 Show active roadmap in AI status; 8e3f3d7 Implement failure feedback fingerprint phase; 855cc45 Design lightweight failure feedback loop; efc0a03 Fix release metadata verification completion
- upgrade/install の追加作業や互換性を壊す変更はありません。
- tag / push / GitHub release の公開操作は明示承認済みです。

## Release Notes (English)

v0.7.8 improves AI-facing status output, review evidence, localized human-approval messages, and the failure-log feedback foundation.

### Changes

- Improved `nilo status --ai` with clearer active-roadmap state and next-action guidance.
- Localized human-approval messages using the project's `primary_language`.
- Hardened Claude ReviewResult import and evidence output for reviews that have not run or still have unresolved findings.
- Added failure fingerprints and a read-only shadow report for the lightweight failure-feedback Phase 1.
- Fixed release-metadata verification completion and complete CLI test-shard coverage.

### Verification

- Changed check: `PYTHONPATH=src python tests/run_shards.py --changed --jobs auto` — passed.
- Full check: `PYTHONPATH=src python tests/run_shards.py --all --jobs auto` — passed (`verification_5298d550d511`).
- Change range: `v0.7.7..HEAD`; commits: e8fc80e Improve active roadmap status display; 854fcfb Localize human approval messages; 1375ea7 Harden Claude ReviewResult import; 949b20b Clarify review evidence status; 93a7659 Show active roadmap in AI status; 8e3f3d7 Implement failure feedback fingerprint phase; 855cc45 Design lightweight failure feedback loop; efc0a03 Fix release metadata verification completion
- No additional upgrade/install steps or breaking compatibility changes are required.
- Explicit approval was received for tag, push, and GitHub release creation.
